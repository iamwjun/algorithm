# Definition for singly-linked list.
class ListNode:
    def __init__(self, x):
        self.val = x
        self.next = None

one = ListNode(0)
cur = one
node = ListNode(add)






class Solution:
    def addTwoNumbers(self, l1, l2):
        print(l1(0))
        node = l2.head
        while node:
            print(node.value)
            node = node.next
        for node in l1:
            print(node.value)

Solution().addTwoNumbers(one, two)